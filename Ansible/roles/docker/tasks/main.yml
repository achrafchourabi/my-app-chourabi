---
# tasks file for docker
- name: create image docker
  docker_image:
    name: devops
    build: /root/my-app-chourabi/Dockerfile
     dest: /var/lib/jenkins/workspace/devops
     pull: no
    state: present
    force_source: true

